`ifndef _{{ env_name | upper }}_ENVIRONMENT_SV
`define _{{ env_name | upper }}_ENVIRONMENT_SV

class {{ env_name }}_environment;

    `uvm_component_utils({{ env_name }}_environment)

    extern function new(string name = "{{ env_name }}_environment", uvm_component parent = null);
    
endclass

function {{ env_name }}_environment::new(string name, uvm_component parent);
    super.new(name, parent);
endfunction

`endif